#include<stdio.h>

int main()
{
   int num1,num2,max;
    printf("Enter the value of num1 and num2:\n");
    scanf("%d %d", num1,num2);
    if(num1>num2)
    {    
        max=num2;
        printf("The maximum no. is:%d",max);
     }
    else
    {
        max=num2;
       
        printf("The maximum. no. is:%d",max);
    }
    return(0);
} 
